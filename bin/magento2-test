#!/bin/bash

DIR=${1?Error: no extension dir given}

php-cs-fixer fix --allow-risky=yes $DIR --dry-run

VENDOR_PATH=$(cd $DIR/../.. && pwd)
phpstan analyse --autoload-file=$VENDOR_PATH/autoload.php $DIR
